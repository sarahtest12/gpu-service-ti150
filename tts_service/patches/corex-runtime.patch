diff --git a/cosyvoice/cli/cosyvoice.py b/cosyvoice/cli/cosyvoice.py
index 7ab04a7..8a75103 100644
--- a/cosyvoice/cli/cosyvoice.py
+++ b/cosyvoice/cli/cosyvoice.py
@@ -19 +18,0 @@ from hyperpyyaml import load_hyperpyyaml
-from modelscope import snapshot_download
@@ -33 +32 @@ class CosyVoice:
-            model_dir = snapshot_download(model_dir)
+            raise FileNotFoundError('CosyVoice model directory not found: {}'.format(model_dir))
@@ -145 +144 @@ class CosyVoice2(CosyVoice):
-            model_dir = snapshot_download(model_dir)
+            raise FileNotFoundError('CosyVoice model directory not found: {}'.format(model_dir))
@@ -195 +194 @@ class CosyVoice3(CosyVoice2):
-            model_dir = snapshot_download(model_dir)
+            raise FileNotFoundError('CosyVoice model directory not found: {}'.format(model_dir))
@@ -230 +229 @@ def AutoModel(**kwargs):
-        kwargs['model_dir'] = snapshot_download(kwargs['model_dir'])
+        raise FileNotFoundError('CosyVoice model directory not found: {}'.format(kwargs['model_dir']))
diff --git a/cosyvoice/cli/frontend.py b/cosyvoice/cli/frontend.py
index 6d397cc..6ce258d 100644
--- a/cosyvoice/cli/frontend.py
+++ b/cosyvoice/cli/frontend.py
@@ -47,2 +47 @@ class CosyVoiceFrontEnd:
-                                                                     providers=["CUDAExecutionProvider" if torch.cuda.is_available() else
-                                                                                "CPUExecutionProvider"])
+                                                                     providers=["CPUExecutionProvider"])
diff --git a/cosyvoice/cli/model.py b/cosyvoice/cli/model.py
index 92a15d9..a5eb530 100644
--- a/cosyvoice/cli/model.py
+++ b/cosyvoice/cli/model.py
@@ -15,0 +16 @@ import os
+from concurrent.futures import CancelledError
@@ -121,8 +122,11 @@ class CosyVoiceModel:
-            for i in token_generator:
-                if i in self.silent_tokens:
-                    cur_silent_token_num += 1
-                    if cur_silent_token_num > max_silent_token_num:
-                        continue
-                else:
-                    cur_silent_token_num = 0
-                self.tts_speech_token_dict[uuid].append(i)
+            try:
+                for i in token_generator:
+                    if i in self.silent_tokens:
+                        cur_silent_token_num += 1
+                        if cur_silent_token_num > max_silent_token_num:
+                            continue
+                    else:
+                        cur_silent_token_num = 0
+                    self.tts_speech_token_dict[uuid].append(i)
+            except CancelledError:
+                self.llm_cancelled_dict[uuid] = True
@@ -273,0 +278 @@ class CosyVoice2Model(CosyVoiceModel):
+        self.llm_cancelled_dict = {}
@@ -336,0 +342 @@ class CosyVoice2Model(CosyVoiceModel):
+            self.llm_cancelled_dict[this_uuid] = False
@@ -345 +351,5 @@ class CosyVoice2Model(CosyVoiceModel):
-            prompt_token_pad = int(np.ceil(flow_prompt_speech_token.shape[1] / self.token_hop_len) * self.token_hop_len - flow_prompt_speech_token.shape[1])
+            # Stream hop growth is request-local. Persisting it on the model makes
+            # later utterances on a reusable session wait for progressively more
+            # tokens before their first audio chunk.
+            token_hop_len = self.token_hop_len
+            prompt_token_pad = int(np.ceil(flow_prompt_speech_token.shape[1] / token_hop_len) * token_hop_len - flow_prompt_speech_token.shape[1])
@@ -348 +358,3 @@ class CosyVoice2Model(CosyVoiceModel):
-                this_token_hop_len = self.token_hop_len + prompt_token_pad if token_offset == 0 else self.token_hop_len
+                if self.llm_cancelled_dict[this_uuid] is True:
+                    break
+                this_token_hop_len = token_hop_len + prompt_token_pad if token_offset == 0 else token_hop_len
@@ -360 +372 @@ class CosyVoice2Model(CosyVoiceModel):
-                    self.token_hop_len = min(self.token_max_hop_len, self.token_hop_len * self.stream_scale_factor)
+                    token_hop_len = min(self.token_max_hop_len, token_hop_len * self.stream_scale_factor)
@@ -365,10 +377,11 @@ class CosyVoice2Model(CosyVoiceModel):
-            # deal with remain tokens, make sure inference remain token len equals token_hop_len when cache_speech is not None
-            this_tts_speech_token = torch.tensor(self.tts_speech_token_dict[this_uuid]).unsqueeze(dim=0)
-            this_tts_speech = self.token2wav(token=this_tts_speech_token,
-                                             prompt_token=flow_prompt_speech_token,
-                                             prompt_feat=prompt_speech_feat,
-                                             embedding=flow_embedding,
-                                             token_offset=token_offset,
-                                             uuid=this_uuid,
-                                             finalize=True)
-            yield {'tts_speech': this_tts_speech.cpu()}
+            if self.llm_cancelled_dict[this_uuid] is False:
+                # deal with remain tokens, make sure inference remain token len equals token_hop_len when cache_speech is not None
+                this_tts_speech_token = torch.tensor(self.tts_speech_token_dict[this_uuid]).unsqueeze(dim=0)
+                this_tts_speech = self.token2wav(token=this_tts_speech_token,
+                                                 prompt_token=flow_prompt_speech_token,
+                                                 prompt_feat=prompt_speech_feat,
+                                                 embedding=flow_embedding,
+                                                 token_offset=token_offset,
+                                                 uuid=this_uuid,
+                                                 finalize=True)
+                yield {'tts_speech': this_tts_speech.cpu()}
@@ -378,10 +391,11 @@ class CosyVoice2Model(CosyVoiceModel):
-            this_tts_speech_token = torch.tensor(self.tts_speech_token_dict[this_uuid]).unsqueeze(dim=0)
-            this_tts_speech = self.token2wav(token=this_tts_speech_token,
-                                             prompt_token=flow_prompt_speech_token,
-                                             prompt_feat=prompt_speech_feat,
-                                             embedding=flow_embedding,
-                                             token_offset=0,
-                                             uuid=this_uuid,
-                                             finalize=True,
-                                             speed=speed)
-            yield {'tts_speech': this_tts_speech.cpu()}
+            if self.llm_cancelled_dict[this_uuid] is False:
+                this_tts_speech_token = torch.tensor(self.tts_speech_token_dict[this_uuid]).unsqueeze(dim=0)
+                this_tts_speech = self.token2wav(token=this_tts_speech_token,
+                                                 prompt_token=flow_prompt_speech_token,
+                                                 prompt_feat=prompt_speech_feat,
+                                                 embedding=flow_embedding,
+                                                 token_offset=0,
+                                                 uuid=this_uuid,
+                                                 finalize=True,
+                                                 speed=speed)
+                yield {'tts_speech': this_tts_speech.cpu()}
@@ -390,0 +405 @@ class CosyVoice2Model(CosyVoiceModel):
+            self.llm_cancelled_dict.pop(this_uuid)
@@ -420,0 +436 @@ class CosyVoice3Model(CosyVoice2Model):
+        self.llm_cancelled_dict = {}
diff --git a/cosyvoice/hifigan/generator.py b/cosyvoice/hifigan/generator.py
index bbc2a21..b0b9df1 100644
--- a/cosyvoice/hifigan/generator.py
+++ b/cosyvoice/hifigan/generator.py
@@ -715,3 +715,5 @@ class CausalHiFTGenerator(HiFTGenerator):
-        # mel->f0 NOTE f0_predictor precision is crucial for causal inference, move self.f0_predictor to cpu if necessary
-        self.f0_predictor.to(torch.float64)
-        f0 = self.f0_predictor(speech_feat.to(torch.float64), finalize=finalize).to(speech_feat)
+        # mel->f0 NOTE f0_predictor precision is crucial for causal inference.
+        # CoreX IXDNN rejects float64 Conv1d, so retain float64 precision on CPU.
+        self.f0_predictor.to(device='cpu', dtype=torch.float64)
+        f0 = self.f0_predictor(speech_feat.to(device='cpu', dtype=torch.float64),
+                               finalize=finalize).to(speech_feat)
